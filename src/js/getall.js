export function get(ele) {
    return document.querySelector(ele);
}
export function getAll(element) {
    return document.querySelectorAll(element);
}
export function getId(id) {
    return document.getElementById(id);
}
